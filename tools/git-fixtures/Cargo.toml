[package]
name = "git-fixtures"
description = "Deterministic git repository fixtures for the e2e verification harness. Not part of the product."
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
git2.workspace = true
git-core = { path = "../../crates/git-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
